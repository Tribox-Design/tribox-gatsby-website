backend:
  name: github
  repo: totzk9/tribox-gatsby-website
  branch: develop

media_folder: content/assets
public_folder: /assets

collections:
  - name: work
    label: Work
    folder: content/work
    create: true
    path: "{{title}}/index"
    fields:
      - { name: date, label: Date, widget: datetime }
      - { name: title, label: Title }
      - { name: thumbnail, label: Cover Photo, widget: image }
      - { name: description, label: Description, widget: text }
      - { name: client, label: Client }
      - { name: services, label: Services }
      - name: "featuredOn"
        label: "Featured On"
        widget: "list"
        summary: '{{fields.featured.name}} - {{fields.featured.link}}'
        required: false
        fields:
          - label: Featured
            name: featured
            widget: object
            fields:
              - {label: Name, name: name, widget: string, default: "World Brand Design Society"}
              - {label: Link, name: link, widget: string, required: false }
      - {
          name: isHighlighted,
          label: Is Highlighted,
          widget: boolean,
          default: false,
        }
      - {
          name: isPublishedWork,
          label: Publish Work,
          widget: boolean,
          default: true,
        }
      - { name: body, label: Body, widget: markdown }
  - name: blog
    label: Blog
    folder: content/blog
    create: true
    path: "{{title}}/index"
    fields:
      - { name: date, label: Date, widget: datetime }
      - { name: title, label: Title, widget: string }
      - { name: description, label: Description, widget: string }
      - { name: thumbnail, label: Cover Photo, widget: image }
      - {
          name: isPublishedBlog,
          label: Publish Blog,
          widget: boolean,
          default: true,
        }
      - { name: body, label: Body, widget: markdown }
